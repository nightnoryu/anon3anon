services:
  anon3anon:
    environment:
      # Token for your bot, obtained from t.me/BotFather
      ANON3ANON_TELEGRAM_BOT_TOKEN: "123:ABC"
      # Comma-separated Telegram user IDs allowed to register as recipients.
      # Leave empty to let anyone register.
      ANON3ANON_ALLOWED_USER_IDS: ""
